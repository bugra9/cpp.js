#| -*- mode: perl; -*-
#| Configure target for platform:'wasi' (wasm32-wasip3); toolchain and flags arrive via env - this pins only what env cannot.
my %targets = (
    "wasi-p3" => {
        inherit_from     => [ "BASE_unix" ],
        sys_id           => "WASI",
        bn_ops           => "THIRTY_TWO_BIT",
        thread_scheme    => "(unknown)",
        perlasm_scheme   => "void",
        shared_target    => "",
        shared_extension => "",
    },
);
