# @cpp.js/package-sqlite3-wasm

The **WebAssembly** build of the precompiled **SQLite3** library, built with [cpp.js](https://cpp.js.org).

> For installation and usage, see the main package: **[@cpp.js/package-sqlite3](https://www.npmjs.com/package/@cpp.js/package-sqlite3)**.

## Supported targets
- WebAssembly (`wasm32`) — single-threaded
- WebAssembly (`wasm32`) — multi-threaded (needs `SharedArrayBuffer`; serve with COOP + COEP headers)

## License
This project includes the precompiled SQLite3 library, released into the [public domain](https://www.sqlite.org/copyright.html).
