// The hub. Its body (one card grid per sidebar section) is generated in ../nav.js, so this file
// only carries the copy that is genuinely its own.
export default {
    slug: '',
    title: 'Guide',
    description: 'Every guide page, grouped by what you are trying to do.',
    lede: 'Getting started walks from an empty directory to C++ running in your app. Concepts explain the pieces you will meet after that - the build, the bindings, the filesystem, threads. Everything here is a page you can read end to end in a few minutes.',
};
